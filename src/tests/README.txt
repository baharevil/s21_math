! В случаях проблем с тестами необходимо использовать команду:
    
    apt install check

После этого тесты должны запускаться спокойно. По поводу самих тестов, я их не писал, взял с git чтобы просто потестить функции :)